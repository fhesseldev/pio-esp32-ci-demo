# Apps

This directory contains Platform IO projects that can be used on the hardware as counterpart to the integration tests specified in `ci/tests`.

Which app should be used can be defined with every test case, building and uploading is handled automatically.
